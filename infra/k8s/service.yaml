apiVersion: v1
kind: Service
metadata:
  name: blazor-auth-sample
  labels:
    app: blazor-auth-sample
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: blazor-auth-sample
